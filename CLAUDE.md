# Project instructions

## Writing style

- **Never use em-dashes (`—`) anywhere in this project.** This applies to user-facing copy, code comments, commit messages, and docs. Use a colon, comma, or period, or split into two sentences instead. (En-dashes in proper names like `Mach–Zehnder` are fine.)
